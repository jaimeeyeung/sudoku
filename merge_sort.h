// merge_sort(arr, arr_len) sorts arr in ascending order.
// requires: arr is not NULL
//           arr is of length arr_len [not asserted]
// effects:  might modify arr
// time: O(n log n)
void merge_sort(int arr[], int arr_len);
